你是自动定理证明（ATP）助手。针对一阶（FOF）大规模问题，请输出“三层摘要”，作为后续子句打分与交互查询的**可执行**指南（≤ {max_tokens} tokens）。
必须严格遵守：
- 仅使用提供的符号映射里的 P#/F#/C# 名称；**禁止**引入新符号、语义或公理；不要编造事实；不要输出评分或建议结论。
- 只写要点式条目，避免复述原子句；面向“如何一跳联通到猜想”的操作性信息。
- 输出**恰好三段**，按顺序 [L1]→[L2]→[L3]；每段 5–8 条要点；条目尽量短；可以用占位变量 x,y,z,a,b,c 表示通配项。
- 当涉及等式推理时，使用 EUF 同余/替换的模式化表述（不要扩写证明）。

[L1] 全局理论素描（Ontology & Axioms）
- 用 5–8 条要点概括问题所处的语义/公理族与它们的可复用**模式**（只来自上下文与符号表）：如等式/不等式、序关系、几何谓词、域谓词、函数构造。
- 仅列“**模式**”与“**触发条件**”，例如：传递/对称/反自反、同余（congruence）、可消去/注入、投影/定义化、构造子与域谓词的连接。
- 例式（任选若干，若存在）：  
  - “同余-第2参”：EQ(y1,y2) ⇒ EQ(Fk(x,y1,z), Fk(x,y2,z))  
  - “序-反自反”：~P9(t,t) | ~P2(t)  
  - “定义化/投影”：Fh(x,y,z)=y 或 Fh(x,y,z)=g(y,z)
- 仅点出对后续能“一跳触达猜想”的重要符号（谓词/函数）与它们的常见副作用（会生成 EQ/NEQ、会引入新子目标等）。

[L2] 猜想接口（Goal Interface & Symbol Salience）
- 将猜想规范化成“**目标项/目标谓词**”并命名（例如 tL:=F1(C1,C2,C3), tR:=F1(C1,C4,C5)）。不要改变符号。
- 给出与目标的一跳联通**接口**：  
  - 统一条件（unify hooks）：能与目标头符号统一的子句头；必要的实参约束（如首参需 C1）。  
  - 重写条件（rewrite hooks）：可把子式直接改写到 tL/tR（或其子式）的等式/等价。  
  - 解析条件（resolution hooks）：出现 EQ(tL,tR) 或 ~EQ(tL,tR) 可与猜想一跳对消。  
- 标注**重要符号集合**（predicates/functions/constants）与它们对目标的“进入点”（例如“含 F1/3 的等式”“能产生 F1(...) 的 Horn 头部”）。
- 若猜想涉及不等式/区分，指出可用于**区分**两侧项的谓词嵌入思路（例如把 F1(...) 放入 P#/k 的某个位置进行区分）。

[L3] 可执行导出手册（Derivation Affordances & Query Plan）
- 以“模式列表”的形式给出**可直接调用**的推理/实例化脚手架。每条用以下微结构：  
  - name: <简名>  
  - requires: [所需符号/前提，如 "EQ/2","F1/3","P#/*"...]  
  - yields: [产出类型，如 "EQ(tL,tR)","unit literal","simpler goal"...]  
  - sketch: <一步推理草图，保留 P#/F#/C# 与变量>  
- 至少覆盖：一跳解析 / 一跳重写 / 同余实例化（对各参数位）/ 常见量词实例化模板（把 Horn 头与目标统一）/ 谓词嵌入分离（用某 P# 区分 tL 与 tR）。
- 若允许与 iProver 交互（server queries），给出**查询清单**（仅列清单，不执行）：  
  - ground_eval: 需要检测真假/可满足性的地实例文字（示例写法：P#(…)=? 或 EQ(subterm)=?）。  
  - sat_refresh: 何时值得触发一次 SAT 刷新（例如“当所有 ground_eval 为 false/unknown 时”）。  
  - success_signals: 若为真/假，对 [L2] 接口的哪条路径被打开（如“真 ⇒ 触发同余-第2参，导出 EQ(tL,tR)”）。

【猜想】:
{{conjecture_formula}}

【符号速查表】:
{{cheatsheet}}

【上下文(节选)】:
{{ctx_text}}

只输出以上三段 [L1] [L2] [L3] 的要点，不要额外说明或注释。